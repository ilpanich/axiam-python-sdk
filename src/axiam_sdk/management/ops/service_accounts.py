# @generated by scripts/gen_management.py from management-registry.json and
# openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
# `python scripts/gen_management.py`; CI verifies the committed output is current.

"""The ``service_accounts`` management namespace (CONTRACT §27.1).

Machine identities, their secrets, and the certificate a device-bound one
authenticates with.
"""

from __future__ import annotations

import builtins
from typing import TYPE_CHECKING

from axiam_sdk.management import models
from axiam_sdk.management._page import (
    Page,
    PageRequest,
    collect_pages,
    collect_pages_async,
    page_of,
    page_query,
)
from axiam_sdk.management._request import (
    ManagementCall,
    send_management,
    send_management_async,
)
from axiam_sdk.management._scope import (
    NamespaceScope,
    require_uuid,
)

if TYPE_CHECKING:  # pragma: no cover - typing only
    from axiam_sdk._async_client import AsyncAxiamClient
    from axiam_sdk._client import AxiamClient, _AxiamClientBase


def _call_list(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    page: PageRequest | None = None,
) -> ManagementCall:
    """Build the ``service_accounts.list`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    return ManagementCall(
        operation="service_accounts.list",
        method="GET",
        path_template="/api/v1/service-accounts",
        path="/api/v1/service-accounts",
        query={**page_query(page)},
    )


def _call_create(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    body: models.CreateServiceAccountRequest,
) -> ManagementCall:
    """Build the ``service_accounts.create`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    return ManagementCall(
        operation="service_accounts.create",
        method="POST",
        path_template="/api/v1/service-accounts",
        path="/api/v1/service-accounts",
        body=body.to_wire(),
    )


def _call_get(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    sa_id: str,
) -> ManagementCall:
    """Build the ``service_accounts.get`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    sa_id = require_uuid(sa_id, "sa_id", "service_accounts.get")
    return ManagementCall(
        operation="service_accounts.get",
        method="GET",
        path_template="/api/v1/service-accounts/{sa_id}",
        path=f"/api/v1/service-accounts/{sa_id}",
    )


def _call_update(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    sa_id: str,
    body: models.UpdateServiceAccount,
) -> ManagementCall:
    """Build the ``service_accounts.update`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    sa_id = require_uuid(sa_id, "sa_id", "service_accounts.update")
    return ManagementCall(
        operation="service_accounts.update",
        method="PUT",
        path_template="/api/v1/service-accounts/{sa_id}",
        path=f"/api/v1/service-accounts/{sa_id}",
        body=body.to_wire(),
    )


def _call_delete(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    sa_id: str,
) -> ManagementCall:
    """Build the ``service_accounts.delete`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    sa_id = require_uuid(sa_id, "sa_id", "service_accounts.delete")
    return ManagementCall(
        operation="service_accounts.delete",
        method="DELETE",
        path_template="/api/v1/service-accounts/{sa_id}",
        path=f"/api/v1/service-accounts/{sa_id}",
    )


def _call_rotate_secret(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    sa_id: str,
) -> ManagementCall:
    """Build the ``service_accounts.rotate_secret`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    sa_id = require_uuid(sa_id, "sa_id", "service_accounts.rotate_secret")
    return ManagementCall(
        operation="service_accounts.rotate_secret",
        method="POST",
        path_template="/api/v1/service-accounts/{sa_id}/rotate-secret",
        path=f"/api/v1/service-accounts/{sa_id}/rotate-secret",
    )


def _call_bind_certificate(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    sa_id: str,
    body: models.BindCertificate,
) -> ManagementCall:
    """Build the ``service_accounts.bind_certificate`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    sa_id = require_uuid(sa_id, "sa_id", "service_accounts.bind_certificate")
    return ManagementCall(
        operation="service_accounts.bind_certificate",
        method="POST",
        path_template="/api/v1/service-accounts/{sa_id}/bind-certificate",
        path=f"/api/v1/service-accounts/{sa_id}/bind-certificate",
        body=body.to_wire(),
    )


def _call_list_roles(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    service_account_id: str,
) -> ManagementCall:
    """Build the ``service_accounts.list_roles`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    service_account_id = require_uuid(
        service_account_id, "service_account_id", "service_accounts.list_roles"
    )
    return ManagementCall(
        operation="service_accounts.list_roles",
        method="GET",
        path_template="/api/v1/service-accounts/{service_account_id}/roles",
        path=f"/api/v1/service-accounts/{service_account_id}/roles",
    )


def _call_list_groups(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    service_account_id: str,
) -> ManagementCall:
    """Build the ``service_accounts.list_groups`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    service_account_id = require_uuid(
        service_account_id, "service_account_id", "service_accounts.list_groups"
    )
    return ManagementCall(
        operation="service_accounts.list_groups",
        method="GET",
        path_template="/api/v1/service-accounts/{service_account_id}/groups",
        path=f"/api/v1/service-accounts/{service_account_id}/groups",
    )


class ServiceAccountsApi:
    """The ``service_accounts`` namespace handle.

    Machine identities, their secrets, and the certificate a device-bound
    one authenticates with.

    Acquiring one performs no I/O and allocates nothing meaningful (§27.2
    rule 1); it holds the client and cannot be constructed without one.
    """

    def __init__(self, client: AxiamClient, scope: NamespaceScope | None = None) -> None:
        """Bind the handle to ``client``; reached as a client property."""
        self._client = client
        self._scope = scope or NamespaceScope()

    def list(self, page: PageRequest | None = None) -> Page[models.ServiceAccountResponse]:
        """``GET /api/v1/service-accounts``"""
        raw = send_management(
            self._client,
            _call_list(self._client, self._scope, page),
        )
        return page_of(raw, models.ServiceAccountResponse)

    def list_all(
        self,
        start: PageRequest | None = None,
    ) -> builtins.list[models.ServiceAccountResponse]:
        """Walk ``service_accounts.list`` to exhaustion, concatenating every
        page.

        The auto-paging form §27.4 rule 4 requires. It stops on an empty
        page even if ``total`` disagrees, so a misreporting server costs one
        wasted request rather than an unbounded loop.
        """
        return collect_pages(start, lambda p: self.list(p))

    def create(
        self,
        body: models.CreateServiceAccountRequest,
    ) -> models.ServiceAccountCreatedResponse:
        """``POST /api/v1/service-accounts``

        **Returns secret material, once.** ``client_secret`` is returned by
        this call and by no other; no later ``get`` will return it again,
        and the ``get`` projection has no field where it was. Discarding the
        result destroys the credential (§27.5 rule 3).

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_create(self._client, self._scope, body),
        )
        return models.ServiceAccountCreatedResponse.model_validate(raw)

    def get(self, sa_id: str) -> models.ServiceAccountResponse:
        """``GET /api/v1/service-accounts/{sa_id}``"""
        raw = send_management(
            self._client,
            _call_get(self._client, self._scope, sa_id),
        )
        return models.ServiceAccountResponse.model_validate(raw)

    def update(
        self,
        sa_id: str,
        body: models.UpdateServiceAccount,
    ) -> models.ServiceAccountResponse:
        """``PUT /api/v1/service-accounts/{sa_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_update(self._client, self._scope, sa_id, body),
        )
        return models.ServiceAccountResponse.model_validate(raw)

    def delete(self, sa_id: str) -> None:
        """``DELETE /api/v1/service-accounts/{sa_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        send_management(
            self._client,
            _call_delete(self._client, self._scope, sa_id),
        )

    def rotate_secret(self, sa_id: str) -> models.RotateSecretResponse:
        """``POST /api/v1/service-accounts/{sa_id}/rotate-secret``

        **Returns secret material, once.** ``client_secret`` is returned by
        this call and by no other; no later ``get`` will return it again,
        and the ``get`` projection has no field where it was. Discarding the
        result destroys the credential (§27.5 rule 3).

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_rotate_secret(self._client, self._scope, sa_id),
        )
        return models.RotateSecretResponse.model_validate(raw)

    def bind_certificate(self, sa_id: str, body: models.BindCertificate) -> None:
        """``POST /api/v1/service-accounts/{sa_id}/bind-certificate``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        send_management(
            self._client,
            _call_bind_certificate(self._client, self._scope, sa_id, body),
        )

    def list_roles(self, service_account_id: str) -> builtins.list[models.RoleAssignment]:
        """``GET /api/v1/service-accounts/{service_account_id}/roles``"""
        raw = send_management(
            self._client,
            _call_list_roles(self._client, self._scope, service_account_id),
        )
        return [models.RoleAssignment.model_validate(item) for item in raw or []]

    def list_groups(self, service_account_id: str) -> builtins.list[models.Group]:
        """``GET /api/v1/service-accounts/{service_account_id}/groups``"""
        raw = send_management(
            self._client,
            _call_list_groups(self._client, self._scope, service_account_id),
        )
        return [models.Group.model_validate(item) for item in raw or []]


class AsyncServiceAccountsApi:
    """The ``service_accounts`` namespace handle, async form.

    Machine identities, their secrets, and the certificate a device-bound
    one authenticates with.

    Acquiring one performs no I/O and allocates nothing meaningful (§27.2
    rule 1); it holds the client and cannot be constructed without one.
    """

    def __init__(self, client: AsyncAxiamClient, scope: NamespaceScope | None = None) -> None:
        """Bind the handle to ``client``; reached as a client property."""
        self._client = client
        self._scope = scope or NamespaceScope()

    async def list(self, page: PageRequest | None = None) -> Page[models.ServiceAccountResponse]:
        """``GET /api/v1/service-accounts``"""
        raw = await send_management_async(
            self._client,
            _call_list(self._client, self._scope, page),
        )
        return page_of(raw, models.ServiceAccountResponse)

    async def list_all(
        self,
        start: PageRequest | None = None,
    ) -> builtins.list[models.ServiceAccountResponse]:
        """Walk ``service_accounts.list`` to exhaustion, concatenating every
        page.

        The auto-paging form §27.4 rule 4 requires. It stops on an empty
        page even if ``total`` disagrees, so a misreporting server costs one
        wasted request rather than an unbounded loop.
        """
        return await collect_pages_async(start, lambda p: self.list(p))

    async def create(
        self,
        body: models.CreateServiceAccountRequest,
    ) -> models.ServiceAccountCreatedResponse:
        """``POST /api/v1/service-accounts``

        **Returns secret material, once.** ``client_secret`` is returned by
        this call and by no other; no later ``get`` will return it again,
        and the ``get`` projection has no field where it was. Discarding the
        result destroys the credential (§27.5 rule 3).

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_create(self._client, self._scope, body),
        )
        return models.ServiceAccountCreatedResponse.model_validate(raw)

    async def get(self, sa_id: str) -> models.ServiceAccountResponse:
        """``GET /api/v1/service-accounts/{sa_id}``"""
        raw = await send_management_async(
            self._client,
            _call_get(self._client, self._scope, sa_id),
        )
        return models.ServiceAccountResponse.model_validate(raw)

    async def update(
        self,
        sa_id: str,
        body: models.UpdateServiceAccount,
    ) -> models.ServiceAccountResponse:
        """``PUT /api/v1/service-accounts/{sa_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_update(self._client, self._scope, sa_id, body),
        )
        return models.ServiceAccountResponse.model_validate(raw)

    async def delete(self, sa_id: str) -> None:
        """``DELETE /api/v1/service-accounts/{sa_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        await send_management_async(
            self._client,
            _call_delete(self._client, self._scope, sa_id),
        )

    async def rotate_secret(self, sa_id: str) -> models.RotateSecretResponse:
        """``POST /api/v1/service-accounts/{sa_id}/rotate-secret``

        **Returns secret material, once.** ``client_secret`` is returned by
        this call and by no other; no later ``get`` will return it again,
        and the ``get`` projection has no field where it was. Discarding the
        result destroys the credential (§27.5 rule 3).

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_rotate_secret(self._client, self._scope, sa_id),
        )
        return models.RotateSecretResponse.model_validate(raw)

    async def bind_certificate(self, sa_id: str, body: models.BindCertificate) -> None:
        """``POST /api/v1/service-accounts/{sa_id}/bind-certificate``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        await send_management_async(
            self._client,
            _call_bind_certificate(self._client, self._scope, sa_id, body),
        )

    async def list_roles(self, service_account_id: str) -> builtins.list[models.RoleAssignment]:
        """``GET /api/v1/service-accounts/{service_account_id}/roles``"""
        raw = await send_management_async(
            self._client,
            _call_list_roles(self._client, self._scope, service_account_id),
        )
        return [models.RoleAssignment.model_validate(item) for item in raw or []]

    async def list_groups(self, service_account_id: str) -> builtins.list[models.Group]:
        """``GET /api/v1/service-accounts/{service_account_id}/groups``"""
        raw = await send_management_async(
            self._client,
            _call_list_groups(self._client, self._scope, service_account_id),
        )
        return [models.Group.model_validate(item) for item in raw or []]
