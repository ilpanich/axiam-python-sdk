# @generated by scripts/gen_management.py from management-registry.json and
# openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
# `python scripts/gen_management.py`; CI verifies the committed output is current.

"""The ``tenants`` management namespace (CONTRACT §27.1).

Tenants within an organization -- the isolation boundary every other
namespace is scoped to.
"""

from __future__ import annotations

import builtins
from typing import TYPE_CHECKING

from axiam_sdk.management import models
from axiam_sdk.management._page import (
    Page,
    PageRequest,
    collect_pages,
    collect_pages_async,
    page_of,
    page_query,
)
from axiam_sdk.management._request import (
    ManagementCall,
    send_management,
    send_management_async,
)
from axiam_sdk.management._scope import (
    NamespaceScope,
    require_uuid,
    resolve_org,
)

if TYPE_CHECKING:  # pragma: no cover - typing only
    from axiam_sdk._async_client import AsyncAxiamClient
    from axiam_sdk._client import AxiamClient, _AxiamClientBase


def _call_list(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    page: PageRequest | None = None,
) -> ManagementCall:
    """Build the ``tenants.list`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "tenants.list")
    return ManagementCall(
        operation="tenants.list",
        method="GET",
        path_template="/api/v1/organizations/{org_id}/tenants",
        path=f"/api/v1/organizations/{org_id}/tenants",
        query={**page_query(page)},
    )


def _call_create(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    body: models.CreateTenantRequest,
) -> ManagementCall:
    """Build the ``tenants.create`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "tenants.create")
    return ManagementCall(
        operation="tenants.create",
        method="POST",
        path_template="/api/v1/organizations/{org_id}/tenants",
        path=f"/api/v1/organizations/{org_id}/tenants",
        body=body.to_wire(),
    )


def _call_get(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    tenant_id: str,
) -> ManagementCall:
    """Build the ``tenants.get`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "tenants.get")
    tenant_id = require_uuid(tenant_id, "tenant_id", "tenants.get")
    return ManagementCall(
        operation="tenants.get",
        method="GET",
        path_template="/api/v1/organizations/{org_id}/tenants/{tenant_id}",
        path=f"/api/v1/organizations/{org_id}/tenants/{tenant_id}",
    )


def _call_update(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    tenant_id: str,
    body: models.UpdateTenant,
) -> ManagementCall:
    """Build the ``tenants.update`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "tenants.update")
    tenant_id = require_uuid(tenant_id, "tenant_id", "tenants.update")
    return ManagementCall(
        operation="tenants.update",
        method="PUT",
        path_template="/api/v1/organizations/{org_id}/tenants/{tenant_id}",
        path=f"/api/v1/organizations/{org_id}/tenants/{tenant_id}",
        body=body.to_wire(),
    )


def _call_delete(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    tenant_id: str,
) -> ManagementCall:
    """Build the ``tenants.delete`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "tenants.delete")
    tenant_id = require_uuid(tenant_id, "tenant_id", "tenants.delete")
    return ManagementCall(
        operation="tenants.delete",
        method="DELETE",
        path_template="/api/v1/organizations/{org_id}/tenants/{tenant_id}",
        path=f"/api/v1/organizations/{org_id}/tenants/{tenant_id}",
    )


class TenantsApi:
    """The ``tenants`` namespace handle.

    Tenants within an organization -- the isolation boundary every other
    namespace is scoped to.

    Acquiring one performs no I/O and allocates nothing meaningful (§27.2
    rule 1); it holds the client and cannot be constructed without one.
    """

    def __init__(self, client: AxiamClient, scope: NamespaceScope | None = None) -> None:
        """Bind the handle to ``client``; reached as a client property."""
        self._client = client
        self._scope = scope or NamespaceScope()

    def in_org(self, org_id: str) -> TenantsApi:
        """Address a different organization than the client's own.

        §27.4 rule 3: the client's ``org_id`` is the default, and a
        platform-admin token legitimately overrides it. Returns a new
        handle; the original is unchanged.
        """
        return TenantsApi(self._client, self._scope.with_org(org_id))

    def list(self, page: PageRequest | None = None) -> Page[models.Tenant]:
        """``GET /api/v1/organizations/{org_id}/tenants``"""
        raw = send_management(
            self._client,
            _call_list(self._client, self._scope, page),
        )
        return page_of(raw, models.Tenant)

    def list_all(self, start: PageRequest | None = None) -> builtins.list[models.Tenant]:
        """Walk ``tenants.list`` to exhaustion, concatenating every page.

        The auto-paging form §27.4 rule 4 requires. It stops on an empty
        page even if ``total`` disagrees, so a misreporting server costs one
        wasted request rather than an unbounded loop.
        """
        return collect_pages(start, lambda p: self.list(p))

    def create(self, body: models.CreateTenantRequest) -> models.Tenant:
        """``POST /api/v1/organizations/{org_id}/tenants``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_create(self._client, self._scope, body),
        )
        return models.Tenant.model_validate(raw)

    def get(self, tenant_id: str) -> models.Tenant:
        """``GET /api/v1/organizations/{org_id}/tenants/{tenant_id}``"""
        raw = send_management(
            self._client,
            _call_get(self._client, self._scope, tenant_id),
        )
        return models.Tenant.model_validate(raw)

    def update(self, tenant_id: str, body: models.UpdateTenant) -> models.Tenant:
        """``PUT /api/v1/organizations/{org_id}/tenants/{tenant_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_update(self._client, self._scope, tenant_id, body),
        )
        return models.Tenant.model_validate(raw)

    def delete(self, tenant_id: str) -> None:
        """``DELETE /api/v1/organizations/{org_id}/tenants/{tenant_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        send_management(
            self._client,
            _call_delete(self._client, self._scope, tenant_id),
        )


class AsyncTenantsApi:
    """The ``tenants`` namespace handle, async form.

    Tenants within an organization -- the isolation boundary every other
    namespace is scoped to.

    Acquiring one performs no I/O and allocates nothing meaningful (§27.2
    rule 1); it holds the client and cannot be constructed without one.
    """

    def __init__(self, client: AsyncAxiamClient, scope: NamespaceScope | None = None) -> None:
        """Bind the handle to ``client``; reached as a client property."""
        self._client = client
        self._scope = scope or NamespaceScope()

    def in_org(self, org_id: str) -> AsyncTenantsApi:
        """Address a different organization than the client's own.

        §27.4 rule 3: the client's ``org_id`` is the default, and a
        platform-admin token legitimately overrides it. Returns a new
        handle; the original is unchanged.
        """
        return AsyncTenantsApi(self._client, self._scope.with_org(org_id))

    async def list(self, page: PageRequest | None = None) -> Page[models.Tenant]:
        """``GET /api/v1/organizations/{org_id}/tenants``"""
        raw = await send_management_async(
            self._client,
            _call_list(self._client, self._scope, page),
        )
        return page_of(raw, models.Tenant)

    async def list_all(self, start: PageRequest | None = None) -> builtins.list[models.Tenant]:
        """Walk ``tenants.list`` to exhaustion, concatenating every page.

        The auto-paging form §27.4 rule 4 requires. It stops on an empty
        page even if ``total`` disagrees, so a misreporting server costs one
        wasted request rather than an unbounded loop.
        """
        return await collect_pages_async(start, lambda p: self.list(p))

    async def create(self, body: models.CreateTenantRequest) -> models.Tenant:
        """``POST /api/v1/organizations/{org_id}/tenants``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_create(self._client, self._scope, body),
        )
        return models.Tenant.model_validate(raw)

    async def get(self, tenant_id: str) -> models.Tenant:
        """``GET /api/v1/organizations/{org_id}/tenants/{tenant_id}``"""
        raw = await send_management_async(
            self._client,
            _call_get(self._client, self._scope, tenant_id),
        )
        return models.Tenant.model_validate(raw)

    async def update(self, tenant_id: str, body: models.UpdateTenant) -> models.Tenant:
        """``PUT /api/v1/organizations/{org_id}/tenants/{tenant_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_update(self._client, self._scope, tenant_id, body),
        )
        return models.Tenant.model_validate(raw)

    async def delete(self, tenant_id: str) -> None:
        """``DELETE /api/v1/organizations/{org_id}/tenants/{tenant_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        await send_management_async(
            self._client,
            _call_delete(self._client, self._scope, tenant_id),
        )
