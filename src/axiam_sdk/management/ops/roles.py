# @generated by scripts/gen_management.py from management-registry.json and
# openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
# `python scripts/gen_management.py`; CI verifies the committed output is current.

"""The ``roles`` management namespace (CONTRACT §27.1).

Roles, their permission sets, and their assignment to users and groups.
"""

from __future__ import annotations

import builtins
from typing import TYPE_CHECKING

from axiam_sdk.management import models
from axiam_sdk.management._page import (
    Page,
    PageRequest,
    collect_pages,
    collect_pages_async,
    page_of,
    page_query,
)
from axiam_sdk.management._request import (
    ManagementCall,
    send_management,
    send_management_async,
)
from axiam_sdk.management._scope import (
    NamespaceScope,
    require_uuid,
)

if TYPE_CHECKING:  # pragma: no cover - typing only
    from axiam_sdk._async_client import AsyncAxiamClient
    from axiam_sdk._client import AxiamClient, _AxiamClientBase


def _call_list(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    page: PageRequest | None = None,
) -> ManagementCall:
    """Build the ``roles.list`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    return ManagementCall(
        operation="roles.list",
        method="GET",
        path_template="/api/v1/roles",
        path="/api/v1/roles",
        query={**page_query(page)},
    )


def _call_create(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    body: models.CreateRoleRequest,
) -> ManagementCall:
    """Build the ``roles.create`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    return ManagementCall(
        operation="roles.create",
        method="POST",
        path_template="/api/v1/roles",
        path="/api/v1/roles",
        body=body.to_wire(),
    )


def _call_get(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    role_id: str,
) -> ManagementCall:
    """Build the ``roles.get`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    role_id = require_uuid(role_id, "role_id", "roles.get")
    return ManagementCall(
        operation="roles.get",
        method="GET",
        path_template="/api/v1/roles/{role_id}",
        path=f"/api/v1/roles/{role_id}",
    )


def _call_update(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    role_id: str,
    body: models.UpdateRole,
) -> ManagementCall:
    """Build the ``roles.update`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    role_id = require_uuid(role_id, "role_id", "roles.update")
    return ManagementCall(
        operation="roles.update",
        method="PUT",
        path_template="/api/v1/roles/{role_id}",
        path=f"/api/v1/roles/{role_id}",
        body=body.to_wire(),
    )


def _call_delete(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    role_id: str,
) -> ManagementCall:
    """Build the ``roles.delete`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    role_id = require_uuid(role_id, "role_id", "roles.delete")
    return ManagementCall(
        operation="roles.delete",
        method="DELETE",
        path_template="/api/v1/roles/{role_id}",
        path=f"/api/v1/roles/{role_id}",
    )


def _call_list_users(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    role_id: str,
) -> ManagementCall:
    """Build the ``roles.list_users`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    role_id = require_uuid(role_id, "role_id", "roles.list_users")
    return ManagementCall(
        operation="roles.list_users",
        method="GET",
        path_template="/api/v1/roles/{role_id}/users",
        path=f"/api/v1/roles/{role_id}/users",
    )


def _call_assign_to_user(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    role_id: str,
    body: models.AssignRoleToUserRequest,
) -> ManagementCall:
    """Build the ``roles.assign_to_user`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    role_id = require_uuid(role_id, "role_id", "roles.assign_to_user")
    return ManagementCall(
        operation="roles.assign_to_user",
        method="POST",
        path_template="/api/v1/roles/{role_id}/users",
        path=f"/api/v1/roles/{role_id}/users",
        body=body.to_wire(),
    )


def _call_unassign_from_user(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    role_id: str,
    user_id: str,
    resource_id: str | None = None,
) -> ManagementCall:
    """Build the ``roles.unassign_from_user`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    role_id = require_uuid(role_id, "role_id", "roles.unassign_from_user")
    user_id = require_uuid(user_id, "user_id", "roles.unassign_from_user")
    return ManagementCall(
        operation="roles.unassign_from_user",
        method="DELETE",
        path_template="/api/v1/roles/{role_id}/users/{user_id}",
        path=f"/api/v1/roles/{role_id}/users/{user_id}",
        query={"resource_id": resource_id},
    )


def _call_list_groups(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    role_id: str,
) -> ManagementCall:
    """Build the ``roles.list_groups`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    role_id = require_uuid(role_id, "role_id", "roles.list_groups")
    return ManagementCall(
        operation="roles.list_groups",
        method="GET",
        path_template="/api/v1/roles/{role_id}/groups",
        path=f"/api/v1/roles/{role_id}/groups",
    )


def _call_assign_to_group(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    role_id: str,
    body: models.AssignRoleToGroupRequest,
) -> ManagementCall:
    """Build the ``roles.assign_to_group`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    role_id = require_uuid(role_id, "role_id", "roles.assign_to_group")
    return ManagementCall(
        operation="roles.assign_to_group",
        method="POST",
        path_template="/api/v1/roles/{role_id}/groups",
        path=f"/api/v1/roles/{role_id}/groups",
        body=body.to_wire(),
    )


def _call_unassign_from_group(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    role_id: str,
    group_id: str,
    resource_id: str | None = None,
) -> ManagementCall:
    """Build the ``roles.unassign_from_group`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    role_id = require_uuid(role_id, "role_id", "roles.unassign_from_group")
    group_id = require_uuid(group_id, "group_id", "roles.unassign_from_group")
    return ManagementCall(
        operation="roles.unassign_from_group",
        method="DELETE",
        path_template="/api/v1/roles/{role_id}/groups/{group_id}",
        path=f"/api/v1/roles/{role_id}/groups/{group_id}",
        query={"resource_id": resource_id},
    )


def _call_list_permissions(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    role_id: str,
) -> ManagementCall:
    """Build the ``roles.list_permissions`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    role_id = require_uuid(role_id, "role_id", "roles.list_permissions")
    return ManagementCall(
        operation="roles.list_permissions",
        method="GET",
        path_template="/api/v1/roles/{role_id}/permissions",
        path=f"/api/v1/roles/{role_id}/permissions",
    )


def _call_grant_permission(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    role_id: str,
    body: models.GrantPermissionRequest,
) -> ManagementCall:
    """Build the ``roles.grant_permission`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    role_id = require_uuid(role_id, "role_id", "roles.grant_permission")
    return ManagementCall(
        operation="roles.grant_permission",
        method="POST",
        path_template="/api/v1/roles/{role_id}/permissions",
        path=f"/api/v1/roles/{role_id}/permissions",
        body=body.to_wire(),
    )


def _call_revoke_permission(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    role_id: str,
    permission_id: str,
) -> ManagementCall:
    """Build the ``roles.revoke_permission`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    role_id = require_uuid(role_id, "role_id", "roles.revoke_permission")
    permission_id = require_uuid(permission_id, "permission_id", "roles.revoke_permission")
    return ManagementCall(
        operation="roles.revoke_permission",
        method="DELETE",
        path_template="/api/v1/roles/{role_id}/permissions/{permission_id}",
        path=f"/api/v1/roles/{role_id}/permissions/{permission_id}",
    )


class RolesApi:
    """The ``roles`` namespace handle.

    Roles, their permission sets, and their assignment to users and groups.

    Acquiring one performs no I/O and allocates nothing meaningful (§27.2
    rule 1); it holds the client and cannot be constructed without one.
    """

    def __init__(self, client: AxiamClient, scope: NamespaceScope | None = None) -> None:
        """Bind the handle to ``client``; reached as a client property."""
        self._client = client
        self._scope = scope or NamespaceScope()

    def list(self, page: PageRequest | None = None) -> Page[models.Role]:
        """``GET /api/v1/roles``"""
        raw = send_management(
            self._client,
            _call_list(self._client, self._scope, page),
        )
        return page_of(raw, models.Role)

    def list_all(self, start: PageRequest | None = None) -> builtins.list[models.Role]:
        """Walk ``roles.list`` to exhaustion, concatenating every page.

        The auto-paging form §27.4 rule 4 requires. It stops on an empty
        page even if ``total`` disagrees, so a misreporting server costs one
        wasted request rather than an unbounded loop.
        """
        return collect_pages(start, lambda p: self.list(p))

    def create(self, body: models.CreateRoleRequest) -> models.Role:
        """``POST /api/v1/roles``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_create(self._client, self._scope, body),
        )
        return models.Role.model_validate(raw)

    def get(self, role_id: str) -> models.Role:
        """``GET /api/v1/roles/{role_id}``"""
        raw = send_management(
            self._client,
            _call_get(self._client, self._scope, role_id),
        )
        return models.Role.model_validate(raw)

    def update(self, role_id: str, body: models.UpdateRole) -> models.Role:
        """``PUT /api/v1/roles/{role_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_update(self._client, self._scope, role_id, body),
        )
        return models.Role.model_validate(raw)

    def delete(self, role_id: str) -> None:
        """``DELETE /api/v1/roles/{role_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        send_management(
            self._client,
            _call_delete(self._client, self._scope, role_id),
        )

    def list_users(self, role_id: str) -> builtins.list[models.RoleUserAssignment]:
        """``GET /api/v1/roles/{role_id}/users``"""
        raw = send_management(
            self._client,
            _call_list_users(self._client, self._scope, role_id),
        )
        return [models.RoleUserAssignment.model_validate(item) for item in raw or []]

    def assign_to_user(self, role_id: str, body: models.AssignRoleToUserRequest) -> None:
        """``POST /api/v1/roles/{role_id}/users``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        send_management(
            self._client,
            _call_assign_to_user(self._client, self._scope, role_id, body),
        )

    def unassign_from_user(
        self,
        role_id: str,
        user_id: str,
        resource_id: str | None = None,
    ) -> None:
        """``DELETE /api/v1/roles/{role_id}/users/{user_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        send_management(
            self._client,
            _call_unassign_from_user(self._client, self._scope, role_id, user_id, resource_id),
        )

    def list_groups(self, role_id: str) -> builtins.list[models.RoleGroupAssignment]:
        """``GET /api/v1/roles/{role_id}/groups``"""
        raw = send_management(
            self._client,
            _call_list_groups(self._client, self._scope, role_id),
        )
        return [models.RoleGroupAssignment.model_validate(item) for item in raw or []]

    def assign_to_group(self, role_id: str, body: models.AssignRoleToGroupRequest) -> None:
        """``POST /api/v1/roles/{role_id}/groups``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        send_management(
            self._client,
            _call_assign_to_group(self._client, self._scope, role_id, body),
        )

    def unassign_from_group(
        self,
        role_id: str,
        group_id: str,
        resource_id: str | None = None,
    ) -> None:
        """``DELETE /api/v1/roles/{role_id}/groups/{group_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        send_management(
            self._client,
            _call_unassign_from_group(self._client, self._scope, role_id, group_id, resource_id),
        )

    def list_permissions(self, role_id: str) -> builtins.list[models.ResolvedPermissionGrant]:
        """``GET /api/v1/roles/{role_id}/permissions``"""
        raw = send_management(
            self._client,
            _call_list_permissions(self._client, self._scope, role_id),
        )
        return [models.ResolvedPermissionGrant.model_validate(item) for item in raw or []]

    def grant_permission(self, role_id: str, body: models.GrantPermissionRequest) -> None:
        """``POST /api/v1/roles/{role_id}/permissions``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        send_management(
            self._client,
            _call_grant_permission(self._client, self._scope, role_id, body),
        )

    def revoke_permission(self, role_id: str, permission_id: str) -> None:
        """``DELETE /api/v1/roles/{role_id}/permissions/{permission_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        send_management(
            self._client,
            _call_revoke_permission(self._client, self._scope, role_id, permission_id),
        )


class AsyncRolesApi:
    """The ``roles`` namespace handle, async form.

    Roles, their permission sets, and their assignment to users and groups.

    Acquiring one performs no I/O and allocates nothing meaningful (§27.2
    rule 1); it holds the client and cannot be constructed without one.
    """

    def __init__(self, client: AsyncAxiamClient, scope: NamespaceScope | None = None) -> None:
        """Bind the handle to ``client``; reached as a client property."""
        self._client = client
        self._scope = scope or NamespaceScope()

    async def list(self, page: PageRequest | None = None) -> Page[models.Role]:
        """``GET /api/v1/roles``"""
        raw = await send_management_async(
            self._client,
            _call_list(self._client, self._scope, page),
        )
        return page_of(raw, models.Role)

    async def list_all(self, start: PageRequest | None = None) -> builtins.list[models.Role]:
        """Walk ``roles.list`` to exhaustion, concatenating every page.

        The auto-paging form §27.4 rule 4 requires. It stops on an empty
        page even if ``total`` disagrees, so a misreporting server costs one
        wasted request rather than an unbounded loop.
        """
        return await collect_pages_async(start, lambda p: self.list(p))

    async def create(self, body: models.CreateRoleRequest) -> models.Role:
        """``POST /api/v1/roles``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_create(self._client, self._scope, body),
        )
        return models.Role.model_validate(raw)

    async def get(self, role_id: str) -> models.Role:
        """``GET /api/v1/roles/{role_id}``"""
        raw = await send_management_async(
            self._client,
            _call_get(self._client, self._scope, role_id),
        )
        return models.Role.model_validate(raw)

    async def update(self, role_id: str, body: models.UpdateRole) -> models.Role:
        """``PUT /api/v1/roles/{role_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_update(self._client, self._scope, role_id, body),
        )
        return models.Role.model_validate(raw)

    async def delete(self, role_id: str) -> None:
        """``DELETE /api/v1/roles/{role_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        await send_management_async(
            self._client,
            _call_delete(self._client, self._scope, role_id),
        )

    async def list_users(self, role_id: str) -> builtins.list[models.RoleUserAssignment]:
        """``GET /api/v1/roles/{role_id}/users``"""
        raw = await send_management_async(
            self._client,
            _call_list_users(self._client, self._scope, role_id),
        )
        return [models.RoleUserAssignment.model_validate(item) for item in raw or []]

    async def assign_to_user(self, role_id: str, body: models.AssignRoleToUserRequest) -> None:
        """``POST /api/v1/roles/{role_id}/users``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        await send_management_async(
            self._client,
            _call_assign_to_user(self._client, self._scope, role_id, body),
        )

    async def unassign_from_user(
        self,
        role_id: str,
        user_id: str,
        resource_id: str | None = None,
    ) -> None:
        """``DELETE /api/v1/roles/{role_id}/users/{user_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        await send_management_async(
            self._client,
            _call_unassign_from_user(self._client, self._scope, role_id, user_id, resource_id),
        )

    async def list_groups(self, role_id: str) -> builtins.list[models.RoleGroupAssignment]:
        """``GET /api/v1/roles/{role_id}/groups``"""
        raw = await send_management_async(
            self._client,
            _call_list_groups(self._client, self._scope, role_id),
        )
        return [models.RoleGroupAssignment.model_validate(item) for item in raw or []]

    async def assign_to_group(self, role_id: str, body: models.AssignRoleToGroupRequest) -> None:
        """``POST /api/v1/roles/{role_id}/groups``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        await send_management_async(
            self._client,
            _call_assign_to_group(self._client, self._scope, role_id, body),
        )

    async def unassign_from_group(
        self,
        role_id: str,
        group_id: str,
        resource_id: str | None = None,
    ) -> None:
        """``DELETE /api/v1/roles/{role_id}/groups/{group_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        await send_management_async(
            self._client,
            _call_unassign_from_group(self._client, self._scope, role_id, group_id, resource_id),
        )

    async def list_permissions(self, role_id: str) -> builtins.list[models.ResolvedPermissionGrant]:
        """``GET /api/v1/roles/{role_id}/permissions``"""
        raw = await send_management_async(
            self._client,
            _call_list_permissions(self._client, self._scope, role_id),
        )
        return [models.ResolvedPermissionGrant.model_validate(item) for item in raw or []]

    async def grant_permission(self, role_id: str, body: models.GrantPermissionRequest) -> None:
        """``POST /api/v1/roles/{role_id}/permissions``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        await send_management_async(
            self._client,
            _call_grant_permission(self._client, self._scope, role_id, body),
        )

    async def revoke_permission(self, role_id: str, permission_id: str) -> None:
        """``DELETE /api/v1/roles/{role_id}/permissions/{permission_id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        await send_management_async(
            self._client,
            _call_revoke_permission(self._client, self._scope, role_id, permission_id),
        )
