# @generated by scripts/gen_management.py from management-registry.json and
# openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
# `python scripts/gen_management.py`; CI verifies the committed output is current.

"""The ``federation`` management namespace (CONTRACT §27.1).

Upstream IdP configuration and the per-user links it produces.
"""

from __future__ import annotations

import builtins
from typing import TYPE_CHECKING

from axiam_sdk.management import models
from axiam_sdk.management._page import (
    Page,
    PageRequest,
    collect_pages,
    collect_pages_async,
    page_of,
    page_query,
)
from axiam_sdk.management._request import (
    ManagementCall,
    send_management,
    send_management_async,
)
from axiam_sdk.management._scope import (
    NamespaceScope,
    require_uuid,
)

if TYPE_CHECKING:  # pragma: no cover - typing only
    from axiam_sdk._async_client import AsyncAxiamClient
    from axiam_sdk._client import AxiamClient, _AxiamClientBase


def _call_list_configs(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    page: PageRequest | None = None,
) -> ManagementCall:
    """Build the ``federation.list_configs`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    return ManagementCall(
        operation="federation.list_configs",
        method="GET",
        path_template="/api/v1/federation-configs",
        path="/api/v1/federation-configs",
        query={**page_query(page)},
    )


def _call_create_config(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    body: models.CreateFederationConfigRequest,
) -> ManagementCall:
    """Build the ``federation.create_config`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    return ManagementCall(
        operation="federation.create_config",
        method="POST",
        path_template="/api/v1/federation-configs",
        path="/api/v1/federation-configs",
        body=body.to_wire(),
    )


def _call_get_config(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    id: str,
) -> ManagementCall:
    """Build the ``federation.get_config`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    id = require_uuid(id, "id", "federation.get_config")
    return ManagementCall(
        operation="federation.get_config",
        method="GET",
        path_template="/api/v1/federation-configs/{id}",
        path=f"/api/v1/federation-configs/{id}",
    )


def _call_update_config(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    id: str,
    body: models.UpdateFederationConfigRequest,
) -> ManagementCall:
    """Build the ``federation.update_config`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    id = require_uuid(id, "id", "federation.update_config")
    return ManagementCall(
        operation="federation.update_config",
        method="PUT",
        path_template="/api/v1/federation-configs/{id}",
        path=f"/api/v1/federation-configs/{id}",
        body=body.to_wire(),
    )


def _call_delete_config(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    id: str,
) -> ManagementCall:
    """Build the ``federation.delete_config`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    id = require_uuid(id, "id", "federation.delete_config")
    return ManagementCall(
        operation="federation.delete_config",
        method="DELETE",
        path_template="/api/v1/federation-configs/{id}",
        path=f"/api/v1/federation-configs/{id}",
    )


def _call_list_user_links(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    user_id: str,
) -> ManagementCall:
    """Build the ``federation.list_user_links`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    user_id = require_uuid(user_id, "user_id", "federation.list_user_links")
    return ManagementCall(
        operation="federation.list_user_links",
        method="GET",
        path_template="/api/v1/federation-links/user/{user_id}",
        path=f"/api/v1/federation-links/user/{user_id}",
    )


def _call_delete_link(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    id: str,
) -> ManagementCall:
    """Build the ``federation.delete_link`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    id = require_uuid(id, "id", "federation.delete_link")
    return ManagementCall(
        operation="federation.delete_link",
        method="DELETE",
        path_template="/api/v1/federation-links/{id}",
        path=f"/api/v1/federation-links/{id}",
    )


def _call_oidc_authorize(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    body: models.OidcAuthorizeRequest,
) -> ManagementCall:
    """Build the ``federation.oidc_authorize`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    return ManagementCall(
        operation="federation.oidc_authorize",
        method="POST",
        path_template="/api/v1/federation/oidc/authorize",
        path="/api/v1/federation/oidc/authorize",
        body=body.to_wire(),
    )


def _call_oidc_callback(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    body: models.OidcCallbackRequest,
) -> ManagementCall:
    """Build the ``federation.oidc_callback`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    return ManagementCall(
        operation="federation.oidc_callback",
        method="POST",
        path_template="/api/v1/federation/oidc/callback",
        path="/api/v1/federation/oidc/callback",
        body=body.to_wire(),
    )


class FederationApi:
    """The ``federation`` namespace handle.

    Upstream IdP configuration and the per-user links it produces.

    Acquiring one performs no I/O and allocates nothing meaningful (§27.2
    rule 1); it holds the client and cannot be constructed without one.
    """

    def __init__(self, client: AxiamClient, scope: NamespaceScope | None = None) -> None:
        """Bind the handle to ``client``; reached as a client property."""
        self._client = client
        self._scope = scope or NamespaceScope()

    def list_configs(
        self,
        page: PageRequest | None = None,
    ) -> Page[models.FederationConfigResponse]:
        """``GET /api/v1/federation-configs``"""
        raw = send_management(
            self._client,
            _call_list_configs(self._client, self._scope, page),
        )
        return page_of(raw, models.FederationConfigResponse)

    def list_configs_all(
        self,
        start: PageRequest | None = None,
    ) -> builtins.list[models.FederationConfigResponse]:
        """Walk ``federation.list_configs`` to exhaustion, concatenating every
        page.

        The auto-paging form §27.4 rule 4 requires. It stops on an empty
        page even if ``total`` disagrees, so a misreporting server costs one
        wasted request rather than an unbounded loop.
        """
        return collect_pages(start, lambda p: self.list_configs(p))

    def create_config(
        self,
        body: models.CreateFederationConfigRequest,
    ) -> models.FederationConfigResponse:
        """``POST /api/v1/federation-configs``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_create_config(self._client, self._scope, body),
        )
        return models.FederationConfigResponse.model_validate(raw)

    def get_config(self, id: str) -> models.FederationConfigResponse:
        """``GET /api/v1/federation-configs/{id}``"""
        raw = send_management(
            self._client,
            _call_get_config(self._client, self._scope, id),
        )
        return models.FederationConfigResponse.model_validate(raw)

    def update_config(
        self,
        id: str,
        body: models.UpdateFederationConfigRequest,
    ) -> models.FederationConfigResponse:
        """``PUT /api/v1/federation-configs/{id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_update_config(self._client, self._scope, id, body),
        )
        return models.FederationConfigResponse.model_validate(raw)

    def delete_config(self, id: str) -> None:
        """``DELETE /api/v1/federation-configs/{id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        send_management(
            self._client,
            _call_delete_config(self._client, self._scope, id),
        )

    def list_user_links(self, user_id: str) -> builtins.list[models.FederationLinkResponse]:
        """``GET /api/v1/federation-links/user/{user_id}``"""
        raw = send_management(
            self._client,
            _call_list_user_links(self._client, self._scope, user_id),
        )
        return [models.FederationLinkResponse.model_validate(item) for item in raw or []]

    def delete_link(self, id: str) -> None:
        """``DELETE /api/v1/federation-links/{id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        send_management(
            self._client,
            _call_delete_link(self._client, self._scope, id),
        )

    def oidc_authorize(self, body: models.OidcAuthorizeRequest) -> models.OidcAuthorizeResponse:
        """``POST /api/v1/federation/oidc/authorize``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_oidc_authorize(self._client, self._scope, body),
        )
        return models.OidcAuthorizeResponse.model_validate(raw)

    def oidc_callback(self, body: models.OidcCallbackRequest) -> models.OidcCallbackResponse:
        """``POST /api/v1/federation/oidc/callback``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_oidc_callback(self._client, self._scope, body),
        )
        return models.OidcCallbackResponse.model_validate(raw)


class AsyncFederationApi:
    """The ``federation`` namespace handle, async form.

    Upstream IdP configuration and the per-user links it produces.

    Acquiring one performs no I/O and allocates nothing meaningful (§27.2
    rule 1); it holds the client and cannot be constructed without one.
    """

    def __init__(self, client: AsyncAxiamClient, scope: NamespaceScope | None = None) -> None:
        """Bind the handle to ``client``; reached as a client property."""
        self._client = client
        self._scope = scope or NamespaceScope()

    async def list_configs(
        self,
        page: PageRequest | None = None,
    ) -> Page[models.FederationConfigResponse]:
        """``GET /api/v1/federation-configs``"""
        raw = await send_management_async(
            self._client,
            _call_list_configs(self._client, self._scope, page),
        )
        return page_of(raw, models.FederationConfigResponse)

    async def list_configs_all(
        self,
        start: PageRequest | None = None,
    ) -> builtins.list[models.FederationConfigResponse]:
        """Walk ``federation.list_configs`` to exhaustion, concatenating every
        page.

        The auto-paging form §27.4 rule 4 requires. It stops on an empty
        page even if ``total`` disagrees, so a misreporting server costs one
        wasted request rather than an unbounded loop.
        """
        return await collect_pages_async(start, lambda p: self.list_configs(p))

    async def create_config(
        self,
        body: models.CreateFederationConfigRequest,
    ) -> models.FederationConfigResponse:
        """``POST /api/v1/federation-configs``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_create_config(self._client, self._scope, body),
        )
        return models.FederationConfigResponse.model_validate(raw)

    async def get_config(self, id: str) -> models.FederationConfigResponse:
        """``GET /api/v1/federation-configs/{id}``"""
        raw = await send_management_async(
            self._client,
            _call_get_config(self._client, self._scope, id),
        )
        return models.FederationConfigResponse.model_validate(raw)

    async def update_config(
        self,
        id: str,
        body: models.UpdateFederationConfigRequest,
    ) -> models.FederationConfigResponse:
        """``PUT /api/v1/federation-configs/{id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_update_config(self._client, self._scope, id, body),
        )
        return models.FederationConfigResponse.model_validate(raw)

    async def delete_config(self, id: str) -> None:
        """``DELETE /api/v1/federation-configs/{id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        await send_management_async(
            self._client,
            _call_delete_config(self._client, self._scope, id),
        )

    async def list_user_links(self, user_id: str) -> builtins.list[models.FederationLinkResponse]:
        """``GET /api/v1/federation-links/user/{user_id}``"""
        raw = await send_management_async(
            self._client,
            _call_list_user_links(self._client, self._scope, user_id),
        )
        return [models.FederationLinkResponse.model_validate(item) for item in raw or []]

    async def delete_link(self, id: str) -> None:
        """``DELETE /api/v1/federation-links/{id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        await send_management_async(
            self._client,
            _call_delete_link(self._client, self._scope, id),
        )

    async def oidc_authorize(
        self,
        body: models.OidcAuthorizeRequest,
    ) -> models.OidcAuthorizeResponse:
        """``POST /api/v1/federation/oidc/authorize``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_oidc_authorize(self._client, self._scope, body),
        )
        return models.OidcAuthorizeResponse.model_validate(raw)

    async def oidc_callback(self, body: models.OidcCallbackRequest) -> models.OidcCallbackResponse:
        """``POST /api/v1/federation/oidc/callback``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_oidc_callback(self._client, self._scope, body),
        )
        return models.OidcCallbackResponse.model_validate(raw)
