# @generated by scripts/gen_management.py from management-registry.json and
# openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
# `python scripts/gen_management.py`; CI verifies the committed output is current.

"""The ``oauth2_clients`` management namespace (CONTRACT §27.1).

Registered OAuth2/OIDC clients -- the registration half of what §12, §21 and
§26 then speak to.
"""

from __future__ import annotations

import builtins
from typing import TYPE_CHECKING

from axiam_sdk.management import models
from axiam_sdk.management._page import (
    Page,
    PageRequest,
    collect_pages,
    collect_pages_async,
    page_of,
    page_query,
)
from axiam_sdk.management._request import (
    ManagementCall,
    send_management,
    send_management_async,
)
from axiam_sdk.management._scope import (
    NamespaceScope,
    require_uuid,
)

if TYPE_CHECKING:  # pragma: no cover - typing only
    from axiam_sdk._async_client import AsyncAxiamClient
    from axiam_sdk._client import AxiamClient, _AxiamClientBase


def _call_list(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    page: PageRequest | None = None,
) -> ManagementCall:
    """Build the ``oauth2_clients.list`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    return ManagementCall(
        operation="oauth2_clients.list",
        method="GET",
        path_template="/api/v1/oauth2-clients",
        path="/api/v1/oauth2-clients",
        query={**page_query(page)},
    )


def _call_create(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    body: models.CreateOAuth2ClientRequest,
) -> ManagementCall:
    """Build the ``oauth2_clients.create`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    return ManagementCall(
        operation="oauth2_clients.create",
        method="POST",
        path_template="/api/v1/oauth2-clients",
        path="/api/v1/oauth2-clients",
        body=body.to_wire(),
    )


def _call_get(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    id: str,
) -> ManagementCall:
    """Build the ``oauth2_clients.get`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    id = require_uuid(id, "id", "oauth2_clients.get")
    return ManagementCall(
        operation="oauth2_clients.get",
        method="GET",
        path_template="/api/v1/oauth2-clients/{id}",
        path=f"/api/v1/oauth2-clients/{id}",
    )


def _call_update(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    id: str,
    body: models.UpdateOAuth2ClientRequest,
) -> ManagementCall:
    """Build the ``oauth2_clients.update`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    id = require_uuid(id, "id", "oauth2_clients.update")
    return ManagementCall(
        operation="oauth2_clients.update",
        method="PUT",
        path_template="/api/v1/oauth2-clients/{id}",
        path=f"/api/v1/oauth2-clients/{id}",
        body=body.to_wire(),
    )


def _call_delete(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    id: str,
) -> ManagementCall:
    """Build the ``oauth2_clients.delete`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    id = require_uuid(id, "id", "oauth2_clients.delete")
    return ManagementCall(
        operation="oauth2_clients.delete",
        method="DELETE",
        path_template="/api/v1/oauth2-clients/{id}",
        path=f"/api/v1/oauth2-clients/{id}",
    )


class Oauth2ClientsApi:
    """The ``oauth2_clients`` namespace handle.

    Registered OAuth2/OIDC clients -- the registration half of what §12, §21
    and §26 then speak to.

    Acquiring one performs no I/O and allocates nothing meaningful (§27.2
    rule 1); it holds the client and cannot be constructed without one.
    """

    def __init__(self, client: AxiamClient, scope: NamespaceScope | None = None) -> None:
        """Bind the handle to ``client``; reached as a client property."""
        self._client = client
        self._scope = scope or NamespaceScope()

    def list(self, page: PageRequest | None = None) -> Page[models.OAuth2ClientResponse]:
        """``GET /api/v1/oauth2-clients``"""
        raw = send_management(
            self._client,
            _call_list(self._client, self._scope, page),
        )
        return page_of(raw, models.OAuth2ClientResponse)

    def list_all(
        self,
        start: PageRequest | None = None,
    ) -> builtins.list[models.OAuth2ClientResponse]:
        """Walk ``oauth2_clients.list`` to exhaustion, concatenating every
        page.

        The auto-paging form §27.4 rule 4 requires. It stops on an empty
        page even if ``total`` disagrees, so a misreporting server costs one
        wasted request rather than an unbounded loop.
        """
        return collect_pages(start, lambda p: self.list(p))

    def create(self, body: models.CreateOAuth2ClientRequest) -> models.OAuth2ClientCreatedResponse:
        """``POST /api/v1/oauth2-clients``

        **Returns secret material, once.** ``client_secret`` is returned by
        this call and by no other; no later ``get`` will return it again,
        and the ``get`` projection has no field where it was. Discarding the
        result destroys the credential (§27.5 rule 3).

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_create(self._client, self._scope, body),
        )
        return models.OAuth2ClientCreatedResponse.model_validate(raw)

    def get(self, id: str) -> models.OAuth2ClientResponse:
        """``GET /api/v1/oauth2-clients/{id}``"""
        raw = send_management(
            self._client,
            _call_get(self._client, self._scope, id),
        )
        return models.OAuth2ClientResponse.model_validate(raw)

    def update(
        self,
        id: str,
        body: models.UpdateOAuth2ClientRequest,
    ) -> models.OAuth2ClientResponse:
        """``PUT /api/v1/oauth2-clients/{id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_update(self._client, self._scope, id, body),
        )
        return models.OAuth2ClientResponse.model_validate(raw)

    def delete(self, id: str) -> None:
        """``DELETE /api/v1/oauth2-clients/{id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        send_management(
            self._client,
            _call_delete(self._client, self._scope, id),
        )


class AsyncOauth2ClientsApi:
    """The ``oauth2_clients`` namespace handle, async form.

    Registered OAuth2/OIDC clients -- the registration half of what §12, §21
    and §26 then speak to.

    Acquiring one performs no I/O and allocates nothing meaningful (§27.2
    rule 1); it holds the client and cannot be constructed without one.
    """

    def __init__(self, client: AsyncAxiamClient, scope: NamespaceScope | None = None) -> None:
        """Bind the handle to ``client``; reached as a client property."""
        self._client = client
        self._scope = scope or NamespaceScope()

    async def list(self, page: PageRequest | None = None) -> Page[models.OAuth2ClientResponse]:
        """``GET /api/v1/oauth2-clients``"""
        raw = await send_management_async(
            self._client,
            _call_list(self._client, self._scope, page),
        )
        return page_of(raw, models.OAuth2ClientResponse)

    async def list_all(
        self,
        start: PageRequest | None = None,
    ) -> builtins.list[models.OAuth2ClientResponse]:
        """Walk ``oauth2_clients.list`` to exhaustion, concatenating every
        page.

        The auto-paging form §27.4 rule 4 requires. It stops on an empty
        page even if ``total`` disagrees, so a misreporting server costs one
        wasted request rather than an unbounded loop.
        """
        return await collect_pages_async(start, lambda p: self.list(p))

    async def create(
        self,
        body: models.CreateOAuth2ClientRequest,
    ) -> models.OAuth2ClientCreatedResponse:
        """``POST /api/v1/oauth2-clients``

        **Returns secret material, once.** ``client_secret`` is returned by
        this call and by no other; no later ``get`` will return it again,
        and the ``get`` projection has no field where it was. Discarding the
        result destroys the credential (§27.5 rule 3).

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_create(self._client, self._scope, body),
        )
        return models.OAuth2ClientCreatedResponse.model_validate(raw)

    async def get(self, id: str) -> models.OAuth2ClientResponse:
        """``GET /api/v1/oauth2-clients/{id}``"""
        raw = await send_management_async(
            self._client,
            _call_get(self._client, self._scope, id),
        )
        return models.OAuth2ClientResponse.model_validate(raw)

    async def update(
        self,
        id: str,
        body: models.UpdateOAuth2ClientRequest,
    ) -> models.OAuth2ClientResponse:
        """``PUT /api/v1/oauth2-clients/{id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_update(self._client, self._scope, id, body),
        )
        return models.OAuth2ClientResponse.model_validate(raw)

    async def delete(self, id: str) -> None:
        """``DELETE /api/v1/oauth2-clients/{id}``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        await send_management_async(
            self._client,
            _call_delete(self._client, self._scope, id),
        )
