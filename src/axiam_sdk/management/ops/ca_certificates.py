# @generated by scripts/gen_management.py from management-registry.json and
# openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
# `python scripts/gen_management.py`; CI verifies the committed output is current.

"""The ``ca_certificates`` management namespace (CONTRACT §27.1).

Organization CAs and the per-tenant signing CAs chained beneath them.
"""

from __future__ import annotations

import builtins
from typing import TYPE_CHECKING

from axiam_sdk.management import models
from axiam_sdk.management._page import (
    Page,
    PageRequest,
    collect_pages,
    collect_pages_async,
    page_of,
    page_query,
)
from axiam_sdk.management._request import (
    ManagementCall,
    send_management,
    send_management_async,
)
from axiam_sdk.management._scope import (
    NamespaceScope,
    require_uuid,
    resolve_org,
)

if TYPE_CHECKING:  # pragma: no cover - typing only
    from axiam_sdk._async_client import AsyncAxiamClient
    from axiam_sdk._client import AxiamClient, _AxiamClientBase


def _call_list(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    page: PageRequest | None = None,
) -> ManagementCall:
    """Build the ``ca_certificates.list`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "ca_certificates.list")
    return ManagementCall(
        operation="ca_certificates.list",
        method="GET",
        path_template="/api/v1/organizations/{org_id}/ca-certificates",
        path=f"/api/v1/organizations/{org_id}/ca-certificates",
        query={**page_query(page)},
    )


def _call_generate(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    body: models.CreateCaCertificateRequest,
) -> ManagementCall:
    """Build the ``ca_certificates.generate`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "ca_certificates.generate")
    return ManagementCall(
        operation="ca_certificates.generate",
        method="POST",
        path_template="/api/v1/organizations/{org_id}/ca-certificates",
        path=f"/api/v1/organizations/{org_id}/ca-certificates",
        body=body.to_wire(),
    )


def _call_import_ca(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    body: models.ImportCaCertificateRequest,
) -> ManagementCall:
    """Build the ``ca_certificates.import_ca`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "ca_certificates.import_ca")
    return ManagementCall(
        operation="ca_certificates.import_ca",
        method="POST",
        path_template="/api/v1/organizations/{org_id}/ca-certificates/import",
        path=f"/api/v1/organizations/{org_id}/ca-certificates/import",
        body=body.to_wire(),
    )


def _call_get(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    id: str,
) -> ManagementCall:
    """Build the ``ca_certificates.get`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "ca_certificates.get")
    id = require_uuid(id, "id", "ca_certificates.get")
    return ManagementCall(
        operation="ca_certificates.get",
        method="GET",
        path_template="/api/v1/organizations/{org_id}/ca-certificates/{id}",
        path=f"/api/v1/organizations/{org_id}/ca-certificates/{id}",
    )


def _call_revoke(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    id: str,
) -> ManagementCall:
    """Build the ``ca_certificates.revoke`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "ca_certificates.revoke")
    id = require_uuid(id, "id", "ca_certificates.revoke")
    return ManagementCall(
        operation="ca_certificates.revoke",
        method="POST",
        path_template="/api/v1/organizations/{org_id}/ca-certificates/{id}/revoke",
        path=f"/api/v1/organizations/{org_id}/ca-certificates/{id}/revoke",
    )


def _call_migrate_custody(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    id: str,
) -> ManagementCall:
    """Build the ``ca_certificates.migrate_custody`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "ca_certificates.migrate_custody")
    id = require_uuid(id, "id", "ca_certificates.migrate_custody")
    return ManagementCall(
        operation="ca_certificates.migrate_custody",
        method="POST",
        path_template="/api/v1/organizations/{org_id}/ca-certificates/{id}/migrate-custody",
        path=f"/api/v1/organizations/{org_id}/ca-certificates/{id}/migrate-custody",
    )


def _call_set_mtls_trust_anchor(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    id: str,
    body: models.SetMtlsTrustAnchor,
) -> ManagementCall:
    """Build the ``ca_certificates.set_mtls_trust_anchor`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "ca_certificates.set_mtls_trust_anchor")
    id = require_uuid(id, "id", "ca_certificates.set_mtls_trust_anchor")
    return ManagementCall(
        operation="ca_certificates.set_mtls_trust_anchor",
        method="PUT",
        path_template="/api/v1/organizations/{org_id}/ca-certificates/{id}/mtls-trust-anchor",
        path=f"/api/v1/organizations/{org_id}/ca-certificates/{id}/mtls-trust-anchor",
        body=body.to_wire(),
    )


def _call_list_signing_cas(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    tenant_id: str,
    page: PageRequest | None = None,
) -> ManagementCall:
    """Build the ``ca_certificates.list_signing_cas`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "ca_certificates.list_signing_cas")
    tenant_id = require_uuid(tenant_id, "tenant_id", "ca_certificates.list_signing_cas")
    return ManagementCall(
        operation="ca_certificates.list_signing_cas",
        method="GET",
        path_template="/api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas",
        path=f"/api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas",
        query={**page_query(page)},
    )


def _call_generate_signing_ca(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    tenant_id: str,
    body: models.CreateIntermediateCaRequest,
) -> ManagementCall:
    """Build the ``ca_certificates.generate_signing_ca`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "ca_certificates.generate_signing_ca")
    tenant_id = require_uuid(tenant_id, "tenant_id", "ca_certificates.generate_signing_ca")
    return ManagementCall(
        operation="ca_certificates.generate_signing_ca",
        method="POST",
        path_template="/api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas",
        path=f"/api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas",
        body=body.to_wire(),
    )


def _call_sign_signing_ca_csr(
    client: _AxiamClientBase,
    scope: NamespaceScope,
    tenant_id: str,
    body: models.SignIntermediateCsrRequest,
) -> ManagementCall:
    """Build the ``ca_certificates.sign_signing_ca_csr`` call.

    Shared by the sync and async handles so the path, query and body are
    decided in exactly one place.
    """
    org_id = resolve_org(client, scope, "ca_certificates.sign_signing_ca_csr")
    tenant_id = require_uuid(tenant_id, "tenant_id", "ca_certificates.sign_signing_ca_csr")
    return ManagementCall(
        operation="ca_certificates.sign_signing_ca_csr",
        method="POST",
        path_template="/api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas/sign-csr",
        path=f"/api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas/sign-csr",
        body=body.to_wire(),
    )


class CaCertificatesApi:
    """The ``ca_certificates`` namespace handle.

    Organization CAs and the per-tenant signing CAs chained beneath them.

    Acquiring one performs no I/O and allocates nothing meaningful (§27.2
    rule 1); it holds the client and cannot be constructed without one.
    """

    def __init__(self, client: AxiamClient, scope: NamespaceScope | None = None) -> None:
        """Bind the handle to ``client``; reached as a client property."""
        self._client = client
        self._scope = scope or NamespaceScope()

    def in_org(self, org_id: str) -> CaCertificatesApi:
        """Address a different organization than the client's own.

        §27.4 rule 3: the client's ``org_id`` is the default, and a
        platform-admin token legitimately overrides it. Returns a new
        handle; the original is unchanged.
        """
        return CaCertificatesApi(self._client, self._scope.with_org(org_id))

    def list(self, page: PageRequest | None = None) -> Page[models.CaCertificate]:
        """``GET /api/v1/organizations/{org_id}/ca-certificates``"""
        raw = send_management(
            self._client,
            _call_list(self._client, self._scope, page),
        )
        return page_of(raw, models.CaCertificate)

    def list_all(self, start: PageRequest | None = None) -> builtins.list[models.CaCertificate]:
        """Walk ``ca_certificates.list`` to exhaustion, concatenating every
        page.

        The auto-paging form §27.4 rule 4 requires. It stops on an empty
        page even if ``total`` disagrees, so a misreporting server costs one
        wasted request rather than an unbounded loop.
        """
        return collect_pages(start, lambda p: self.list(p))

    def generate(self, body: models.CreateCaCertificateRequest) -> models.GeneratedCaCertificate:
        """``POST /api/v1/organizations/{org_id}/ca-certificates``

        **Returns secret material, once.** ``private_key_pem`` is returned
        by this call and by no other; no later ``get`` will return it again,
        and the ``get`` projection has no field where it was. Discarding the
        result destroys the credential (§27.5 rule 3).

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_generate(self._client, self._scope, body),
        )
        return models.GeneratedCaCertificate.model_validate(raw)

    def import_ca(self, body: models.ImportCaCertificateRequest) -> models.CaCertificate:
        """``POST /api/v1/organizations/{org_id}/ca-certificates/import``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_import_ca(self._client, self._scope, body),
        )
        return models.CaCertificate.model_validate(raw)

    def get(self, id: str) -> models.CaCertificate:
        """``GET /api/v1/organizations/{org_id}/ca-certificates/{id}``"""
        raw = send_management(
            self._client,
            _call_get(self._client, self._scope, id),
        )
        return models.CaCertificate.model_validate(raw)

    def revoke(self, id: str) -> None:
        """``POST /api/v1/organizations/{org_id}/ca-certificates/{id}/revoke``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        send_management(
            self._client,
            _call_revoke(self._client, self._scope, id),
        )

    def migrate_custody(self, id: str) -> models.MigrateCustodyResponse:
        """``POST
        /api/v1/organizations/{org_id}/ca-certificates/{id}/migrate-custody``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_migrate_custody(self._client, self._scope, id),
        )
        return models.MigrateCustodyResponse.model_validate(raw)

    def set_mtls_trust_anchor(
        self,
        id: str,
        body: models.SetMtlsTrustAnchor,
    ) -> models.MtlsTrustAnchorResponse:
        """``PUT
        /api/v1/organizations/{org_id}/ca-certificates/{id}/mtls-trust-anchor``

        **This is a replacement, not a patch** (§27.4 rule 5). Every field
        of the body is required, and what you do not carry over from a prior
        read is not preserved -- it is overwritten. Read first, change the
        field you mean, send the whole thing back.

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_set_mtls_trust_anchor(self._client, self._scope, id, body),
        )
        return models.MtlsTrustAnchorResponse.model_validate(raw)

    def list_signing_cas(
        self,
        tenant_id: str,
        page: PageRequest | None = None,
    ) -> Page[models.CaCertificate]:
        """``GET
        /api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas``
        """
        raw = send_management(
            self._client,
            _call_list_signing_cas(self._client, self._scope, tenant_id, page),
        )
        return page_of(raw, models.CaCertificate)

    def list_signing_cas_all(
        self,
        tenant_id: str,
        start: PageRequest | None = None,
    ) -> builtins.list[models.CaCertificate]:
        """Walk ``ca_certificates.list_signing_cas`` to exhaustion,
        concatenating every page.

        The auto-paging form §27.4 rule 4 requires. It stops on an empty
        page even if ``total`` disagrees, so a misreporting server costs one
        wasted request rather than an unbounded loop.
        """
        return collect_pages(start, lambda p: self.list_signing_cas(tenant_id, p))

    def generate_signing_ca(
        self,
        tenant_id: str,
        body: models.CreateIntermediateCaRequest,
    ) -> models.GeneratedCaCertificate:
        """``POST
        /api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas``

        **Returns secret material, once.** ``private_key_pem`` is returned
        by this call and by no other; no later ``get`` will return it again,
        and the ``get`` projection has no field where it was. Discarding the
        result destroys the credential (§27.5 rule 3).

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_generate_signing_ca(self._client, self._scope, tenant_id, body),
        )
        return models.GeneratedCaCertificate.model_validate(raw)

    def sign_signing_ca_csr(
        self,
        tenant_id: str,
        body: models.SignIntermediateCsrRequest,
    ) -> models.CaCertificate:
        """``POST
        /api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas/sign-csr``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = send_management(
            self._client,
            _call_sign_signing_ca_csr(self._client, self._scope, tenant_id, body),
        )
        return models.CaCertificate.model_validate(raw)


class AsyncCaCertificatesApi:
    """The ``ca_certificates`` namespace handle, async form.

    Organization CAs and the per-tenant signing CAs chained beneath them.

    Acquiring one performs no I/O and allocates nothing meaningful (§27.2
    rule 1); it holds the client and cannot be constructed without one.
    """

    def __init__(self, client: AsyncAxiamClient, scope: NamespaceScope | None = None) -> None:
        """Bind the handle to ``client``; reached as a client property."""
        self._client = client
        self._scope = scope or NamespaceScope()

    def in_org(self, org_id: str) -> AsyncCaCertificatesApi:
        """Address a different organization than the client's own.

        §27.4 rule 3: the client's ``org_id`` is the default, and a
        platform-admin token legitimately overrides it. Returns a new
        handle; the original is unchanged.
        """
        return AsyncCaCertificatesApi(self._client, self._scope.with_org(org_id))

    async def list(self, page: PageRequest | None = None) -> Page[models.CaCertificate]:
        """``GET /api/v1/organizations/{org_id}/ca-certificates``"""
        raw = await send_management_async(
            self._client,
            _call_list(self._client, self._scope, page),
        )
        return page_of(raw, models.CaCertificate)

    async def list_all(
        self,
        start: PageRequest | None = None,
    ) -> builtins.list[models.CaCertificate]:
        """Walk ``ca_certificates.list`` to exhaustion, concatenating every
        page.

        The auto-paging form §27.4 rule 4 requires. It stops on an empty
        page even if ``total`` disagrees, so a misreporting server costs one
        wasted request rather than an unbounded loop.
        """
        return await collect_pages_async(start, lambda p: self.list(p))

    async def generate(
        self,
        body: models.CreateCaCertificateRequest,
    ) -> models.GeneratedCaCertificate:
        """``POST /api/v1/organizations/{org_id}/ca-certificates``

        **Returns secret material, once.** ``private_key_pem`` is returned
        by this call and by no other; no later ``get`` will return it again,
        and the ``get`` projection has no field where it was. Discarding the
        result destroys the credential (§27.5 rule 3).

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_generate(self._client, self._scope, body),
        )
        return models.GeneratedCaCertificate.model_validate(raw)

    async def import_ca(self, body: models.ImportCaCertificateRequest) -> models.CaCertificate:
        """``POST /api/v1/organizations/{org_id}/ca-certificates/import``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_import_ca(self._client, self._scope, body),
        )
        return models.CaCertificate.model_validate(raw)

    async def get(self, id: str) -> models.CaCertificate:
        """``GET /api/v1/organizations/{org_id}/ca-certificates/{id}``"""
        raw = await send_management_async(
            self._client,
            _call_get(self._client, self._scope, id),
        )
        return models.CaCertificate.model_validate(raw)

    async def revoke(self, id: str) -> None:
        """``POST /api/v1/organizations/{org_id}/ca-certificates/{id}/revoke``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        await send_management_async(
            self._client,
            _call_revoke(self._client, self._scope, id),
        )

    async def migrate_custody(self, id: str) -> models.MigrateCustodyResponse:
        """``POST
        /api/v1/organizations/{org_id}/ca-certificates/{id}/migrate-custody``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_migrate_custody(self._client, self._scope, id),
        )
        return models.MigrateCustodyResponse.model_validate(raw)

    async def set_mtls_trust_anchor(
        self,
        id: str,
        body: models.SetMtlsTrustAnchor,
    ) -> models.MtlsTrustAnchorResponse:
        """``PUT
        /api/v1/organizations/{org_id}/ca-certificates/{id}/mtls-trust-anchor``

        **This is a replacement, not a patch** (§27.4 rule 5). Every field
        of the body is required, and what you do not carry over from a prior
        read is not preserved -- it is overwritten. Read first, change the
        field you mean, send the whole thing back.

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_set_mtls_trust_anchor(self._client, self._scope, id, body),
        )
        return models.MtlsTrustAnchorResponse.model_validate(raw)

    async def list_signing_cas(
        self,
        tenant_id: str,
        page: PageRequest | None = None,
    ) -> Page[models.CaCertificate]:
        """``GET
        /api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas``
        """
        raw = await send_management_async(
            self._client,
            _call_list_signing_cas(self._client, self._scope, tenant_id, page),
        )
        return page_of(raw, models.CaCertificate)

    async def list_signing_cas_all(
        self,
        tenant_id: str,
        start: PageRequest | None = None,
    ) -> builtins.list[models.CaCertificate]:
        """Walk ``ca_certificates.list_signing_cas`` to exhaustion,
        concatenating every page.

        The auto-paging form §27.4 rule 4 requires. It stops on an empty
        page even if ``total`` disagrees, so a misreporting server costs one
        wasted request rather than an unbounded loop.
        """
        return await collect_pages_async(start, lambda p: self.list_signing_cas(tenant_id, p))

    async def generate_signing_ca(
        self,
        tenant_id: str,
        body: models.CreateIntermediateCaRequest,
    ) -> models.GeneratedCaCertificate:
        """``POST
        /api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas``

        **Returns secret material, once.** ``private_key_pem`` is returned
        by this call and by no other; no later ``get`` will return it again,
        and the ``get`` projection has no field where it was. Discarding the
        result destroys the credential (§27.5 rule 3).

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_generate_signing_ca(self._client, self._scope, tenant_id, body),
        )
        return models.GeneratedCaCertificate.model_validate(raw)

    async def sign_signing_ca_csr(
        self,
        tenant_id: str,
        body: models.SignIntermediateCsrRequest,
    ) -> models.CaCertificate:
        """``POST
        /api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas/sign-csr``

        Not retried on failure (§27.4 rule 8): every write on this surface
        is issued exactly once, including the ones that look idempotent.
        """
        raw = await send_management_async(
            self._client,
            _call_sign_signing_ca_csr(self._client, self._scope, tenant_id, body),
        )
        return models.CaCertificate.model_validate(raw)
