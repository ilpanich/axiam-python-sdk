# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc
import warnings

from . import userinfo_pb2 as userinfo__pb2

GRPC_GENERATED_VERSION = '1.78.0'
GRPC_VERSION = grpc.__version__
_version_not_supported = False

try:
    from grpc._utilities import first_version_is_lower
    _version_not_supported = first_version_is_lower(GRPC_VERSION, GRPC_GENERATED_VERSION)
except ImportError:
    _version_not_supported = True

if _version_not_supported:
    raise RuntimeError(
        f'The grpc package installed is at version {GRPC_VERSION},'
        + ' but the generated code in userinfo_pb2_grpc.py depends on'
        + f' grpcio>={GRPC_GENERATED_VERSION}.'
        + f' Please upgrade your grpc module to grpcio>={GRPC_GENERATED_VERSION}'
        + f' or downgrade your generated code using grpcio-tools<={GRPC_VERSION}.'
    )


class UserInfoServiceStub(object):
    """OIDC-style userinfo for the *authenticated* caller — the low-latency gRPC
    counterpart of the REST `GET /oauth2/userinfo` endpoint, analogous to
    Zitadel's `zitadel.auth.v1.AuthService/GetMyUser`.

    Identity is derived entirely from the `authorization: Bearer <access_token>`
    metadata header (validated by the server's auth interceptor); the request
    body is empty. The returned claim set mirrors the REST endpoint exactly,
    including OIDC scope gating (see the field comments below).
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.GetUserInfo = channel.unary_unary(
                '/axiam.v1.UserInfoService/GetUserInfo',
                request_serializer=userinfo__pb2.GetUserInfoRequest.SerializeToString,
                response_deserializer=userinfo__pb2.GetUserInfoResponse.FromString,
                _registered_method=True)


class UserInfoServiceServicer(object):
    """OIDC-style userinfo for the *authenticated* caller — the low-latency gRPC
    counterpart of the REST `GET /oauth2/userinfo` endpoint, analogous to
    Zitadel's `zitadel.auth.v1.AuthService/GetMyUser`.

    Identity is derived entirely from the `authorization: Bearer <access_token>`
    metadata header (validated by the server's auth interceptor); the request
    body is empty. The returned claim set mirrors the REST endpoint exactly,
    including OIDC scope gating (see the field comments below).
    """

    def GetUserInfo(self, request, context):
        """Return the authenticated caller's identity claims. `sub`, `tenant_id`, and
        `org_id` are always populated; `email` and `preferred_username` are gated
        on the "email" and "profile" scopes respectively.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_UserInfoServiceServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'GetUserInfo': grpc.unary_unary_rpc_method_handler(
                    servicer.GetUserInfo,
                    request_deserializer=userinfo__pb2.GetUserInfoRequest.FromString,
                    response_serializer=userinfo__pb2.GetUserInfoResponse.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'axiam.v1.UserInfoService', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('axiam.v1.UserInfoService', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class UserInfoService(object):
    """OIDC-style userinfo for the *authenticated* caller — the low-latency gRPC
    counterpart of the REST `GET /oauth2/userinfo` endpoint, analogous to
    Zitadel's `zitadel.auth.v1.AuthService/GetMyUser`.

    Identity is derived entirely from the `authorization: Bearer <access_token>`
    metadata header (validated by the server's auth interceptor); the request
    body is empty. The returned claim set mirrors the REST endpoint exactly,
    including OIDC scope gating (see the field comments below).
    """

    @staticmethod
    def GetUserInfo(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/axiam.v1.UserInfoService/GetUserInfo',
            userinfo__pb2.GetUserInfoRequest.SerializeToString,
            userinfo__pb2.GetUserInfoResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
